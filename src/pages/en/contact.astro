---
import Layout from '@layouts/Layout.astro';
import { getLangFromUrl, useTranslations } from '@i18n/utils';
import * as contactSubHeaderPost from '@content/contact/en/contactSubheader.md';

import ContactSubHeader from '@components/contact/ContactSubHeader.astro';


const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

---

<Layout 
	title={"Contact me"}
	description={"Feel free to contact me for any questions or collaboration."}
	pageTitle={"Contact me"}
	locale={lang}
	>
	<ContactSubHeader data={contactSubHeaderPost} locale={lang} />
</Layout>